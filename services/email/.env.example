### EMAIL SERVICE ENVIRONMENT VARIABLES ###

### MUST CHANGE FOR PRODUCTION ENVIRONMENT ###
APP_ENV=dev

# Email Service Settings
EMAIL_SERVICE_PORT=4002

# Email Provider Configuration
EMAIL_PROVIDER=mailhog  # Options: resend, mailhog, smtp

# SMTP Settings (if EMAIL_PROVIDER=smtp)
SMTP_HOST=smtp.gmail.com
SMTP_USER=client
SMTP_PASS=password
SMTP_PORT=587
EMAIL_FROM=noreply@yourdomain.com

# RESEND Email Provider (if EMAIL_PROVIDER=resend)
RESEND_API_KEY=your_resend_api_key
RESEND_DEFAULT_FROM=noreply@yourdomain.com

# MAILHOG (Development/Testing - if EMAIL_PROVIDER=mailhog)
MAILHOG_HOST=localhost
MAILHOG_PORT=1025
MAILHOG_DEFAULT_FROM=noreply@test.local

# API Security
EMAIL_API_KEY=your-secret-api-key-change-this
