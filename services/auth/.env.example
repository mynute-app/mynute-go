### AUTH SERVICE ENVIRONMENT VARIABLES ###

### MUST CHANGE FOR PRODUCTION ENVIRONMENT ###
APP_ENV=dev

# Auth Service Settings
AUTH_SERVICE_PORT=4001

# PostgreSQL Environment Variables
POSTGRES_HOST=localhost # For dev (localhost) for prod (postgres)
POSTGRES_PORT=5433
POSTGRES_USER=postgres
POSTGRES_PASSWORD=admin # Postgres Password

# IMPORTANT: POSTGRES_DB is used by migration tools and should point to the database
# you want to migrate. For development, set it to devdb. For production, set it to maindb.
# The application uses APP_ENV to determine which database to connect to at runtime.
POSTGRES_DB=devdb  # Change to 'maindb' for production migrations
POSTGRES_DB_TEST=testdb  # Used by application when APP_ENV=test
POSTGRES_DB_DEV=devdb    # Used by application when APP_ENV=dev

# JWT Settings
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Email Provider Settings (for password reset, notifications, etc.)
EMAIL_FROM=noreply@yourdomain.com

# SMTP Settings (if using SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_USER=client
SMTP_PASS=password
SMTP_PORT=587

# RESEND Email Provider
RESEND_API_KEY=your_resend_api_key
RESEND_DEFAULT_FROM=noreply@yourdomain.com

# MAILHOG (Development/Testing Email)
MAILHOG_HOST=localhost
MAILHOG_PORT=1025
MAILHOG_DEFAULT_FROM=noreply@test.local
